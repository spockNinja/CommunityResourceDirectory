# Django
SECRET_KEY=django-insecure-dev-key-for-community-resource-directory-app
DEBUG=True
ALLOWED_HOSTS=*

# Database (set DB_ENGINE to use PostgreSQL instead of the default SQLite)
DB_ENGINE=django.db.backends.postgresql
DB_NAME=community_resource_directory
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
